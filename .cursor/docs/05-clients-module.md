# Task: Clients Module

## Requirements
1. Build the `/clients` view.
2. Implement "Add Client" Modal.
3. Form must handle dynamic arrays for `emails` and `contactNumbers` (allow users to add multiple fields).
4. Prepare UI for future relational mapping to domains and hostings.