# Task: System Users Module

## Requirements
1. Build the `/users` view.
2. **Security:** Restrict this route strictly to the `Admin` role. Redirect `Editor` roles back to `/domains` if accessed.
3. Implement "Add User" Modal.
4. Allow assignment of `Admin` or `Editor` roles.